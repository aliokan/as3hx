package {

    public class Reg {

        private var LABEL_EXP:RegExp = /(\)|\(|\*|\$)/g;

        public function Reg() {
            var urlPattern:RegExp = new RegExp( "http://(www|).*?\.(com|org|net|dev)", "i" );
            new RegExp( patternArray[j], "g");
        }
    }
}
